'use client';

import styled from 'styled-components';

interface LayerAdminMenuProps {}

export const LayerAdminMenuStyled = styled.div<LayerAdminMenuProps>`
  max-width: 375px;
  margin: 0 auto;
`;
