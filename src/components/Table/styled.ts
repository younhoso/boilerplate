'use client';

import styled from 'styled-components';

interface TableProps {}

interface TableHeadProps {}

interface TableHeaderCell {}

export const TableStyled = styled.div<TableProps>``;

export const TableHeadStyled = styled.div<TableHeadProps>``;

export const TableHeaderCellStyled = styled.div<TableHeaderCell>``;

export const TableBodyStyled = styled.div<TableProps>``;

export const TableBodyCellStyled = styled.div<TableProps>``;
